const express = require('express');
const router = express.Router();

// define routes
router.get('/', function(req, res) {
  // ...
});

// more routes can be here

// export the router
module.exports = router;
